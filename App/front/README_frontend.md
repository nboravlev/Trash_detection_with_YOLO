# YOLO Trash Detection — Frontend (React)

Этот проект реализует клиентскую часть системы обнаружения мусора и переполненных баков на основе модели YOLO. Интерфейс позволяет загружать изображения, отправлять их на сервер и визуализировать результаты детекции в удобной форме.

## 📦 Prerequisites

- Node.js и npm должны быть установлены на вашем компьютере

## 🚀 Установка и запуск

1. Создайте новое React-приложение:

```bash
npx create-react-app yolo-detection-frontend
cd yolo-detection-frontend
```

2. Установите необходимые зависимости:

```bash
npm install react-dropzone react-icons
```

3. Замените содержимое `src/App.js` кодом из репозитория. [file](https://github.com/nboravlev/Trash_detection_with_YOLO/raw/main/App/front/App.css)

4. В приложении есть три блока:

   - загрузка изображения и превью
  
     ![image_upload](https://github.com/nboravlev/Trash_detection_with_YOLO/blob/main/Docs/app_upload.PNG)

   - блок с предсказанными классами, bbox'ами и информацией, о том что бак переполнен, либо мусор разбросан
     мимо бака, либо что модель обнаружила опасный объект класса `tires`

     ![detectiom](https://github.com/nboravlev/Trash_detection_with_YOLO/blob/main/Docs/application_det.PNG)


   - блок, где выводится сохраненное изображение с предсказанием, которое встроенный метод **Ultralytics**
     формирует, просто для сравнения.

    ![ultralytics](https://github.com/nboravlev/Trash_detection_with_YOLO/blob/main/Docs/app_yolo.PNG)

   За каждый из блоков отвечает собственный скрипт `jsx` и дизайнерская часть `CSS`. Для корректной работы
   необходимо в папке проекта /src сформировать две папки /components и /style и положить в них
   файлы из репозитория

   ## 📁 Структура проекта

```
src/
├── App.js                       # основной скрипт
├── components/                  # папка с исполнительными файлами 
│   └── DetectionResults.jsx     # скрипт вывода предсказаний
│   └── DropzoneArea.jsx         # скрипт загрузки изображений
│   └── ImagePreview.jsx         # скрипт для предпросмотра
├── style/                       # папка со стиля CSS
│   └── DetectionResult.module.css       
│   └── DropzoneArea.module.css      
│   └── ImagePreview.module.css      
│   └── Shared.module.css      


```
 

6. Запустите сервер разработки:

```bash
npm start
```

Приложение будет доступно по адресу: [http://localhost:3000](http://localhost:3000)

## ⚙️ Важные настройки

### 1. Конфигурация CORS (для backend)

Убедитесь, что сервер FastAPI разрешает подключения с вашего фронтенда. По умолчанию используется адрес `http://localhost:8000`.

### 2. Подключение к API

- Запустите backend-сервер до запуска фронтенда
- Если API работает на другом порту/домене, замените все `http://localhost:8000` в коде на нужный адрес

### 3. Отображение изображений

- Приложение показывает:
  - bounding box'ы, отрисованные на клиенте
  - уведомдение, если на изображении обнаружен объект класса  `tires`. Логика реализована на backend.
  - уведомление, что бак переполнен. Логика реализована на backend.
  - уведомление, если мусор выляется мимо бака. Логика реализована на backend.
  - результирующее изображение, отрисованное сервером
    


## ✨ Функциональность

- Drag-and-drop загрузка изображений
- Превью изображений перед отправкой
- Клиентская отрисовка обнаруженных объектов
- Вывод изображения, обработанного сервером
- Подробная таблица результатов
- Иконка загрузки, обработка ошибок
- Адаптивный дизайн

---

Для корректной работы убедитесь, что ваш backend работает и корректно настроен. 
